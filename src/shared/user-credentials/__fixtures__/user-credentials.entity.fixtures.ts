import type { UserCredentials } from '../user-credentials.entity';

export const mUserCredentialsBaseA: UserCredentials = {
	id: 'userCredentialsId1',
	email: 'shady313@mail.com',
	passwordHash: '',
	userId: 'userId1'
};
